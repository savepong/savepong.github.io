---
// Projects Carousel Section
const projects = [
    {
        icon: "fas fa-layer-group",
        title: "Sekweb.site",
        description: "Architecture for AI website builder (Jul 2023).",
        tag: "AI Platform"
    },
    {
        icon: "fas fa-heartbeat",
        title: "Thai Survive",
        description: "Donation management for crisis relief (Sep 2021).",
        tag: "Social Impact"
    },
    {
        icon: "fas fa-graduation-cap",
        title: "Forenext",
        description: "Enterprise Learning Management System (Aug 2012).",
        tag: "EdTech"
    },
    {
        icon: "fas fa-layer-group",
        title: "Sekweb.site",
        description: "Architecture for AI website builder.",
        tag: "AI Platform"
    },
    {
        icon: "fas fa-heartbeat",
        title: "Thai Survive",
        description: "Donation management for crisis relief.",
        tag: "Social Impact"
    }
];
---

<section id="projects" class="section-screen scroll-mt-28 overflow-hidden pt-10 pb-20">
    <h2 class="text-3xl font-bold mb-8 text-[#1d1d1f] tracking-tight text-center">Projects & Innovations</h2>
    <div class="carousel-container" id="draggable-carousel">
        <div class="carousel-track">
            {projects.map(project => (
                <div class="project-card">
                    <div class="w-12 h-12 rounded-2xl bg-slate-100 flex items-center justify-center text-slate-600 mb-6 border border-slate-200">
                        <i class={`${project.icon} text-xl text-slate-600`}></i>
                    </div>
                    <h4 class="text-xl font-bold text-[#1d1d1f] mb-2">{project.title}</h4>
                    <p class="text-sm text-slate-600 leading-relaxed mb-4">{project.description}</p>
                    <span class="inline-block px-3 py-1 bg-slate-100 text-slate-600 text-xs font-bold rounded-full border border-slate-200">{project.tag}</span>
                </div>
            ))}
        </div>
    </div>
</section>

<script>
    // Draggable carousel logic
    const slider = document.querySelector('.carousel-container');
    let isDown = false;
    let startX: number;
    let scrollLeft: number;

    slider?.addEventListener('mousedown', (e) => {
        isDown = true;
        slider.classList.add('active');
        startX = (e as MouseEvent).pageX - (slider as HTMLElement).offsetLeft;
        scrollLeft = slider.scrollLeft;
    });
    
    slider?.addEventListener('mouseleave', () => {
        isDown = false;
        slider.classList.remove('active');
    });
    
    slider?.addEventListener('mouseup', () => {
        isDown = false;
        slider.classList.remove('active');
    });
    
    slider?.addEventListener('mousemove', (e) => {
        if(!isDown) return;
        e.preventDefault();
        const x = (e as MouseEvent).pageX - (slider as HTMLElement).offsetLeft;
        const walk = (x - startX) * 2;
        slider.scrollLeft = scrollLeft - walk;
    });
</script>
